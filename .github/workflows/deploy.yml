name: Deploy Frontend to Server

on:
  # Kích hoạt khi có code được đẩy lên nhánh main
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        # Bước này sẽ tải mã nguồn từ repo về môi trường của GitHub Actions
        uses: actions/checkout@v3

      - name: Setup Node.js
        # Cài đặt Node.js để có thể chạy npm
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm' # [TỐI ƯU HÓA] Kích hoạt cache cho các gói npm

      - name: Install and Build Frontend
        # Cài đặt thư viện và biên dịch code ra các file tĩnh
        run: |
          echo "📦 Installing frontend dependencies..."
          # Sử dụng 'npm ci' thay vì 'npm install' để cài đặt nhanh và nhất quán hơn từ file lock
          npm ci
          echo "🛠️ Building static files..."
          npm run build

      - name: 🔍 Verify Build Artifacts
        # [DEBUG] Kiểm tra xem thư mục 'dist' có được tạo đúng cách và chứa file không.
        run: |
          echo "--- Contents of dist directory on runner ---"
          ls -laR dist

      - name: Sync files to server with rsync
        uses: appleboy/scp-action@master
        with:          
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_PRIVATE_KEY }}
          port: 22 # Hoặc cổng SSH tùy chỉnh của bạn
          source: "dist/*"
          target: "/home/hoangvietadmin/deploy_temp/frontend_dist"
         

      - name: Activate new version on server        
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_PRIVATE_KEY }}
          port: 22 # Hoặc cổng SSH tùy chỉnh của bạn
          script: |
            echo "--- [DEBUG] Contents of temp directory BEFORE rsync ---"
            # [DEBUG] Kiểm tra xem scp-action đã sao chép file vào thư mục tạm thành công chưa.
            # Nếu thư mục này rỗng, đó là nguyên nhân chính.
            ls -laR /home/hoangvietadmin/deploy_temp/frontend_dist

            echo "🔄 Thay thế các file frontend cũ bằng phiên bản mới..."
            # [TỐI ƯU HÓA] Sử dụng rsync để đồng bộ hóa file một cách hiệu quả.
            # Lệnh này sẽ sao chép file mới, cập nhật file đã thay đổi và xóa file cũ không còn dùng.
            # Thao tác này gần như tức thời, giảm thiểu rủi ro downtime.
            sudo rsync -a --delete /home/hoangvietadmin/deploy_temp/frontend_dist/ /var/www/conghaiso/
            
            # (QUAN TRỌNG) Đảm bảo Nginx có quyền đọc các file mới
            sudo chown -R www-data:www-data /var/www/conghaiso

            echo "--- [DEBUG] Contents of final directory AFTER rsync ---"
            # [DEBUG] Kiểm tra kết quả cuối cùng trong thư mục web.
            ls -laR /var/www/conghaiso

            echo "✅ Triển khai Frontend thành công!"
